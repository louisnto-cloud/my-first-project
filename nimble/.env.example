# OPTIONAL — the app asks for your key in the browser on first run and saves
# it locally, so most people never need this file. Use it only if you prefer
# an environment variable: copy to .env and fill in your key.
ANTHROPIC_API_KEY=sk-ant-...
# Optional: change the port (default 3000)
# PORT=3000
