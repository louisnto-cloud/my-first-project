{
  "name": "etop-platform",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "dev:proto": "npm run dev --workspace apps/web-prototype",
    "dev:api": "npm run dev --workspace apps/api"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "packageManager": "npm@10.9.7"
}
