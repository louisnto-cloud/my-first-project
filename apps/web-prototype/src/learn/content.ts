// Moved to the shared package so the prototype and the platform portal
// use one curriculum source. Re-exported to keep existing imports stable.
export { COURSES, allLessons, recommendedCourse } from '@etop/curriculum';
